.venv/
__pycache__/
.browser-libs/
archive-*/

pages/
# Generated site output at the repo root. The leading slash is load-bearing:
# a bare "sensors/" also matches content/sensors/, which silently ignored new
# sensor files -- the most common contribution this repo can receive.
/sensors/
catalog/
families/
atlas/
framework/
about/
contact/
privacy/
glossary/
categories/
TODO.md
chat-w-gpt.md
index.html
search-index.json
sitemap.xml
robots.txt
rss.xml
404.html
llms.txt
llms-full.txt
# Leading slash: cli/data/sensors.json is committed and must stay that
# way; only the web copy at the repo root is generated output.
/sensors.json
md/
.link-cache.json
*.TMP
.opencode/

# Agent scaffolding and scratch output. Untracked but unignored files are one
# `git add -A` away from public history, and `make deploy`'s old deny-list was
# one forgotten --exclude away from publishing them.
.crush/
scratch/
results/
abstract.md
abstract.sh
.opencode-review.md

# Operator notes: host, key paths, DNS account. Moved out of the public
# AGENTS.md (#118); publishing it is the exact thing it exists to prevent.
OPERATIONS.md

# Citation checker's TTL cache (see scripts/check_citations.py).
.citation-cache.json
